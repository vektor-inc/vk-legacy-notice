{
  "name": "vk-legacy-notice",
  "version": "0.0.1",
  "description": "You can make and register your original custom block patterns.",
  "main": "gulpfile.js",
  "scripts": {
    "update": "ncu -u && npm install",
    "wp-env": "wp-env",
    "build": "gulp replace_text_domain",
    "dist": "composer install --no-dev && gulp dist && composer install",
    "phpunit": "composer install && wp-env run tests-cli --env-cwd=wp-content/plugins/vk-legacy-notice 'phpunit -c .phpunit.xml --testsuite default --verbose'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vektor-inc/vk-legacy-notice.git"
  },
  "author": "Vektor,Inc.",
  "license": "GPL-2.0-or-later",
  "bugs": {
    "url": "https://github.com/vektor-inc/vk-legacy-notice/issues"
  },
  "homepage": "https://github.com/vektor-inc/vk-legacy-notice#readme",
  "devDependencies": {
    "@wordpress/env": "^7.0.0",
    "gulp": "^4.0.2",
    "gulp-replace": "^1.1.4",
    "npm-check-updates": "^16.10.12"
  },
  "volta": {
    "node": "18.16.0"
  }
}
